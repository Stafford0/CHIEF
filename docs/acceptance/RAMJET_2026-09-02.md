# RamJet live acceptance — 2026-09-02

## Scope and safety boundary

- Host: `RAMJET`, Windows, operator account `RAMJET\daria`.
- Starting repository state: clean `main` fast-forwarded to
  `5ceb71d5c2c2595a5a7196a700575e923c57e316`.
- Acceptance branch: `feature/ramjet-live-acceptance`.
- Email sending, browser clicks/forms/login/credential entry/downloads, unrestricted computer
  use, wake word, full-duplex barge-in, and sensitive voice approval remained disabled.
- Protected LAN access remained disabled; the service API stayed on `127.0.0.1:8000`.

## Windows Service and boot recovery

Observed configuration:

| Field | Observation |
| --- | --- |
| Service | `CHIEFRuntime` / `CHIEF Runtime` |
| Identity | `NT AUTHORITY\LocalService` |
| Executable | `C:\Users\daria\Projects\CHIEF\.venv\Scripts\python.exe -m chief.runtime.windows_service` |
| Database | `C:\Users\daria\Projects\CHIEF\data\chief.db` |
| Startup | Automatic |
| Recovery | Restart after 5 seconds, then 15 seconds; reset after 24 hours |
| API | Loopback-only `http://127.0.0.1:8000` |

The host rebooted at `2026-09-02T12:14:02.500Z`. A SYSTEM startup verifier observed the
service running and `/health` online at `2026-09-02T12:18:14.049Z`; a direct post-boot check
then observed `/ready` HTTP 200 with every readiness check true.

Schedule `a67fd6b8-7e18-46e5-af55-d6c2514245e5` survived the reboot with its original next-run
timestamp. The durable emergency stop also survived with `effective_enabled=false` and runtime
status `paused`. After explicit resume, schedule `d9df7a97-9665-4813-9473-123645006af5`
produced event `29d796ed-1990-42b4-9e33-0b6f03ab8478` and verified successful run
`cafdf379-a7fe-44e5-8a19-2ece75ba8e88`.

Unknown event `b4e473dc-6c59-431c-aca5-667102ea5324` used one permitted attempt, entered dead
letter with the missing-handler error, appeared in operator status, was explicitly retried,
failed closed into dead letter again, and was dismissed. Recovery receipts
`be8984e6-49c2-47d9-b323-0e888a3ccd22` and
`d384c373-d1a5-4832-a26a-4ac6304e209f` preserve the operator actions.

## DPAPI vault

A disposable `CHIEF_GMAIL_ACCESS_TOKEN` was created through the live API, rotated, and revoked.
The value was never printed or retained. SQLite dump inspection, repository/source-log search,
and audit JSON inspection found no plaintext. `RAMJET\daria` could not decrypt the ciphertext
created by `LocalService`.

After service restart, the rotated value decrypted under `LocalService`, proven by successful
startup composition of the credential-dependent Gmail metadata and draft-only connector
manifests. No provider call was made. After revocation and restart, secret metadata returned 404
and those manifests were absent.

## Backup, restore, and rollback

While CHIEF was running, the recovery tool created an integrity-checked backup:

- Backup: `C:\Users\daria\Backups\CHIEF\ramjet-20260902\chief.db`
- SHA-256: `81945bdaf96573b77c011ba4b6afc108c8c433335259ecfe74fc326439d52c9a`
- Size: 1,019,904 bytes
- SQLite quick check: `ok`

The backup and manifest were copied to the active OneDrive sync root at
`C:\Users\daria\OneDrive\CHIEF Backups\RamJet\2026-09-02`; the local copy digest matched.
Remote cloud presence and an independently calculated off-device digest remain unverified, so
the corresponding checklist items remain open.

An isolated same-host target successfully staged, verified, and activated the backup. This does
not substitute for the still-open clean/test-machine check. Against the live target, activation
refused an exclusive SQLite lock with the required busy-target error. Offline activation then
preserved the previous database as
`chief.db.pre-restore-20260902T120309Z`, and CHIEF restarted successfully. Restored state passed
SQLite integrity and exposed 99 durable conversations, 308 messages, 11 goals, the approval
tables, evidence/schema tables, and readable run tables.

`/operator/schema-compatibility` returned `integrity_ok=true`, `compatible=true`, no newer
components, and no structural mismatches. A rollback target without a component-version manifest
was rejected; the current v1 manifest was accepted.

## Browser and screenshot evidence

Playwright 1.62 installed Chromium build 1234 into the service-visible directory
`C:\Users\daria\Projects\CHIEF\work\ms-playwright`. Live calls through the `LocalService`
process produced:

- `https://example.com/` research HTTP 200 with text and links labeled `untrusted_external`.
- Screenshot HTTP 200, SHA-256
  `83f3cc87c83f6b8bafd8e41305fd0126544504cec0f1782738099d1be19fa880`, 12,091 bytes,
  1440×900, five-minute expiry, and `persisted=false`.
- Localhost, RFC1918, and link-local targets denied.
- A public HTTP redirect toward a live private canary denied with HTTP 403 before the canary
  received any request.
- Capability receipt kept clicking, form fill, credential entry, downloads, JavaScript
  evaluation, and screenshot persistence false.

The first redirect canary exposed that Chromium request interception could observe the denial
after a private request had already arrived. The branch now fetches bounded HTML through a
pre-follow redirect policy and renders it in a network-isolated, JavaScript-disabled Chromium
context. The repeated live canary received zero private requests.

## Voice and local providers

Windows reported working Turtle Beach speaker/microphone endpoints and Sonic Studio virtual
audio endpoints. CHIEF reported `configured=false` for streaming voice because no STT/TTS
provider adapter or credential is configured. Therefore microphone capture, playback,
transcript streaming, VAD events, and cancellation latency remain unqualified.

The safe text fallback passed live: CHIEF (`qwen3:4b`) replied through local Ollama, and Ultron
(`llama3.1:8b`) also replied. Capability checks confirmed raw audio retention false, VAD action
authorization false, wake word false, full-duplex barge-in false, and sensitive-action voice
approval false. Only text fallback and the required disabled-feature checks are marked complete.

## Verification and remaining live blockers

- Isolated Python suite: 410 passed.
- Ruff: passed.
- React production build: passed.
- Service remained running and healthy through a deliberate transient SQLite exclusive lock
  after runtime retry hardening.

Still live-unqualified: independently verified off-device backup and clean-machine restore;
owner-approved GitHub/Gmail/Calendar/Stripe/ParcelSignals/SMTP/cloud-provider credentials and
their provider-side checks; real STT/TTS capture/playback/streaming/VAD/cancellation tests; and
protected-device LAN/mobile enrollment. These remain implemented-but-not-live-qualified rather
than being promoted to production-ready.
